/*
 * Exercise 9.
 *
 * Find an example where it would make sense to use a name in its own 
 * initializer.
 *
 */

/* Linked list. */
struct node {
	void *data;
	struct node *child;
};

int main(){}
